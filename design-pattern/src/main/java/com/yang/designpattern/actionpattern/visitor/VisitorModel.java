package com.yang.designpattern.actionpattern.visitor;

/**
 * @author: Yang
 * @date: 2019/8/18 11:56
 * @description: 访问者模式【了解即可】
 * 动机：当一个集合内存有多个不同类型的对象时（可能拥有一个共同的父类），由于这些对象的不同，其处理方式也不同，
 * 但每个类都接受对应的访问者的访问，不同访问者的行为时不同的。
 * 定义：表示一个作用于mou对象结构中的各元素的操作，它使我们可以在元素类型的前提下定义多用于元素的新操作。
 * 场景：xml文件解析器设计、编译器设计、复杂集合对象的处理。
 */
public class VisitorModel {
}
